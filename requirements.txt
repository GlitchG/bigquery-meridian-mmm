# Meridian MMM — Dependencies
# Install with: pip install -r requirements.txt
#
# Google Meridian must be installed from GitHub:
#   pip install git+https://github.com/google/meridian.git
#
# Or uncomment the line below and pip will attempt it:
# meridian @ git+https://github.com/google/meridian.git

# Google Meridian runs on TensorFlow Probability (not PyMC/JAX).
# Installing meridian itself pulls in tensorflow + tensorflow-probability;
# they are listed here explicitly for clarity.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
arviz>=0.16
tensorflow>=2.13
tensorflow-probability>=0.21
scipy>=1.10
